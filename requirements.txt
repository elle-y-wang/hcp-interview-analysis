# Python 3.11. Versions are those the analysis was actually run on.

# --- pinned deliberately -----------------------------------------------------
# transformers 5.x removed a loading path the emotion model depends on
# (all_tied_weights_keys), which breaks scripts/05_acoustic_features.py.
transformers==4.46.3
# torch/torchaudio are the CUDA 12.1 builds; install from the pytorch index:
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchaudio==2.5.1

# --- ASR and diarization -----------------------------------------------------
faster-whisper==1.2.1
ctranslate2==4.8.1
funasr==1.4.2
modelscope==1.39.1

# --- acoustic features -------------------------------------------------------
opensmile==2.6.0
soundfile==0.14.0

# --- analysis ----------------------------------------------------------------
pandas==3.0.5
numpy==1.26.4
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.9.0
matplotlib==3.11.1

# --- inputs ------------------------------------------------------------------
python-docx==1.2.0
openpyxl==3.1.5

# ffmpeg / ffprobe are external binaries, expected at $HCP_WORK/bin.
# Thematic coding (scripts/08) shells out to the local `claude` CLI.
